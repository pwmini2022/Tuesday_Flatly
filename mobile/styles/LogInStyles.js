import { StyleSheet } from 'react-native';

export const logInStyles = StyleSheet.create({
  logInView: {
    margin: (0, 35),
    padding: (0, 40),
    backgroundColor: 'white',
    borderRadius: 25,
  },
  title: {
    fontSize: 45,
    textAlign: 'center',
  }
});